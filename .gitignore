node_modules
.svelte-kit
build
dist
.env
.env.*
!.env.example
.DS_Store
# Uploads (never commit media)
uploads
apps/web/uploads

# Nested lockfile — use root package-lock.json for workspaces
apps/web/package-lock.json

